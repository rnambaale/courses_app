stages:
    - build
    - test
    
Job One:
    stage: build
    script: 
        - echo "Building"
        - mkdir build
        - touch build/info.txt
    artifacts:
        paths:
            - build/

Job Two:
    stage: test
    script:
        - echo "Testing"
        - test -f "build/info.txt"
